import { AppContentService } from "carbonldp-panel/my-apps/app-content/app-content.service";
export declare class RolesBrowserView {
    private app;
    canDisplay: boolean;
    constructor(appContentService: AppContentService);
}
